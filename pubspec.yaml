# pubspec.yaml
name: swastik_academy_app
description: A new Flutter project.

environment:
  sdk: '^3.8.0'

dependencies:
  
  flutter:
    sdk: flutter
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9
  firebase_storage: ^12.4.7

  
  url_launcher: ^6.3.1
